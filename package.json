{
  "name": "cazbid",
  "version": "1.0.0",
  "private": true,
  "description": "CazBid — self-contained estimating app (inlined React, pre-compiled JSX) with a Netlify Blobs persistence backend.",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@babel/standalone": "^8.0.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
